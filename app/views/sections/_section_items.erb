<% if current_section.items.size > 0 %>
    <ul class="items">
      <%= render :partial => 'items/item', :collection => current_section.items, :locals => {:context => 'section'} %>
    </ul>
<% else %>
    Add item or drop item from a different section.
<% end %>