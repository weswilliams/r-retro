  <span class="item_vote">(<span id="section_<%= item.section.id %>_item_votes_<%= item.id %>"><%= item.item_votes.size %></span>)</span>
  <span class="item_vote"><%= link_to image_tag("thumbs-up.png", :class => 'nav_image', :alt => "(+1)"), vote_for_retrospective_section_item_url(item.section.retrospective, item.section, item), :remote => true, :method => :post, :class => 'inline_link', :confirm => 'Vote for item?', :title => 'vote for item' %></span>
  <span class="item_vote"><%= link_to image_tag("thumbs-down.png", :class => 'nav_image', :alt => "(-1)"), remove_vote_retrospective_section_item_url(item.section.retrospective, item.section, item), :remote => true, :method => :post, :class => 'inline_link', :confirm => 'Un-vote for item?', :title => 'un-vote for item' %></span>
  <%= link_to image_tag("maximize.png", :class => 'nav_image', :alt => "(show)"), '', :href => "#", :id => "max_inline_section_#{item.section.id}_item_value_#{item.id}", :class => 'inline_link', :title => 'show item' %>
  <%= link_to image_tag("delete.png", :class => 'nav_image', :alt => "(remove)"), [item.section.retrospective, item.section, item], :remote => true, :class => 'inline_link', :method => :delete, :confirm => 'Remove item?', :title => 'remove item' %>
