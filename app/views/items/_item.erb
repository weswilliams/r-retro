
<li class="item">
  <%= item.value %>
  <%= link_to '(edit)', edit_retrospective_section_item_path(item.section.retrospective, item.section, item), :class => 'inline_link' %>
  <%= link_to '(remove)', [item.section.retrospective, item.section, item], :confirm => 'Are you sure?', :method => :delete, :class => 'inline_link' %>
</li>

