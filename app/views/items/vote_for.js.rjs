
page.replace_html("section_#{@item.section.id}_item_votes_#{@item.id}", "#{@item.item_votes.size}")

if (@item.group)
  page.replace_html("group_#{@item.group.id}_item_votes_#{@item.id}", "#{@item.item_votes.size}")
end